# 复制本文件为 .env，并填入你的值（不要把 .env 提交到 Git）

# （端口已在 compose.yaml 写死为 6789:3000）

# 宿主机图片目录（会挂载到容器 /images；用于读取本地图片、写入缩略图缓存等）
IMAGE_DIR_HOST=./images

# 宿主机数据目录（会挂载到容器 /data；用于保存配置文件等，不污染图片目录）
DATA_DIR_HOST=./data

# 管理密码（用于解锁“管理设置”：选择公开相册/数据源等；访客浏览公开相册不需要）
ADMIN_PASS=

# 上传/删除等管理操作已统一由 ADMIN_PASS 解锁（cookie）控制

# Immich（作为远程图片源）
# 例如：https://photo.example.com
IMMICH_URL=
# Immich API Key（仅服务端保存，不要写进仓库/前端）
IMMICH_API_KEY=

# WebDAV 已移除

# 公共相册配置文件路径（管理员选定公开哪个库/相册，会写到这里）
GALLERY_CONFIG_PATH=/data/.gallery_config.json

# 缩略图参数（可选；用于性能与清晰度平衡）
THUMB_WIDTH=480
THUMB_QUALITY=70
